<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_neon_background"
    tools:context=".MainActivity">

    <!-- Header con título neón -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="POKE"
            android:textColor="@color/primary_neon"
            android:textSize="32sp"
            android:textStyle="bold"
            android:fontFamily="monospace" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MATCHER"
            android:textColor="@color/secondary_neon"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="monospace" />

        <View
            android:layout_width="60dp"
            android:layout_height="3dp"
            android:background="@color/accent_neon"
            android:layout_marginTop="8dp" />
    </LinearLayout>

    <!-- Contenedor principal de tarjetas -->
    <FrameLayout
        android:id="@+id/cardContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        app:layout_constraintTop_toBottomOf="@+id/headerLayout"
        app:layout_constraintBottom_toTopOf="@+id/actionButtonsLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.yuyakaido.android.cardstackview.CardStackView
            android:id="@+id/rvTinderPokemon"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:indeterminateTint="@color/primary_neon"
            tools:visibility="visible" />

    </FrameLayout>

    <!-- Indicador de error -->
    <TextView
        android:id="@+id/tvError"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/error_red"
        android:textSize="16sp"
        android:fontFamily="monospace"
        android:gravity="center"
        android:visibility="gone"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/cardContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Controles de acción -->
    <LinearLayout
        android:id="@+id/actionButtonsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="20dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Botón Dislike -->
        <ImageButton
            android:id="@+id/btnDislike"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_close_neon"
            android:background="@drawable/button_neon_secondary"
            android:scaleType="fitCenter"
            android:padding="12dp"
            android:layout_marginEnd="20dp"
            android:contentDescription="Dislike" />

        <!-- Botón Super Like -->
        <ImageButton
            android:id="@+id/btnSuperLike"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_star_neon"
            android:background="@drawable/button_neon_secondary"
            android:scaleType="fitCenter"
            android:padding="12dp"
            android:layout_marginEnd="20dp"
            android:contentDescription="Super Like" />

        <!-- Botón Like -->
        <ImageButton
            android:id="@+id/btnLike"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_heart_neon"
            android:background="@drawable/button_neon_primary"
            android:scaleType="fitCenter"
            android:padding="12dp"
            android:contentDescription="Like" />

    </LinearLayout>

    <!-- Indicador de estado -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Desliza para descubrir Pokémon"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:fontFamily="monospace"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toTopOf="@+id/actionButtonsLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
